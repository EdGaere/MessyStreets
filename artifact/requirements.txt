# MESSY STREETS artefact — pinned runtime dependencies.
#
# Versions are those of the environment that produced the paper (Python 3.12.3,
# mtec-mis-gpu02). Pins are exact, not compatible-release: DuckDB sample
# reproducibility holds within a version and is not guaranteed across them.
#
# The CLI itself (messy_streets/) depends on nothing here — it is stdlib-only so
# that `doctor` can still diagnose a broken dependency install.

# --- data & storage ---
duckdb==1.4.4
pandas==2.2.3
numpy==2.2.6
diskcache==5.6.3

# --- geocoding & geospatial ---
geopy==2.4.1
pygeohash==3.2.2
# python-geohash is deliberately absent. It was pinned in the environment that
# produced the paper, but nothing in the artefact imports it -- every geohash
# call goes through pygeohash, which is pure Python. It was also the only
# dependency without a prebuilt wheel, so dropping it means no compiler is
# needed on any platform. tests/test_credentials.py is not the place that
# checks this; tests/test_doctor.py asserts the wheels-only property.
pycountry==26.2.16
country_converter==1.3.2

# --- text & encoding ---
ftfy==6.3.1
babel==2.17.0

# --- config & expression evaluation ---
hjson==3.1.0
simpleeval==1.0.7
RestrictedPython==7.4
autopep8==2.3.1
dpath==2.2.0

# --- analysis & presentation ---
scikit-learn==1.8.0
tabulate==0.9.0
plotext==5.3.2

# --- runtime plumbing ---
typer==0.24.0
tqdm==4.67.1
httpx==0.28.1
certifi==2025.1.31
colorlog==6.7.0
